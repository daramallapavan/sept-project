package com.example.MergeJavaAndReactBasic.product;

import org.springframework.data.jpa.repository.JpaRepository;

public interface NewImageDataRepository extends JpaRepository<NewImageData,Long> {
}
